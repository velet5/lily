{
  "name": "lily-studio",
  "productName": "Lily Studio",
  "description": "A small desktop studio for LilyPond scores: files, editor and preview in one fixed window.",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "main": "dist/main.js",
  "scripts": {
    "build": "node esbuild.mjs",
    "start": "node esbuild.mjs && electron .",
    "check-types": "tsc --noEmit -p .",
    "test:unit": "node esbuild.mjs --tests && node --test \"out/test/*.test.js\"",
    "test:smoke": "node esbuild.mjs && electron . --smoke-test",
    "test": "npm run test:unit && npm run test:smoke",
    "dist": "node esbuild.mjs --production && APPLE_KEYCHAIN_PROFILE=lily-notary electron-builder --mac dmg && APPLE_KEYCHAIN_PROFILE=lily-notary node scripts/notarize-dmg.mjs",
    "test:e2e": "npm run dist && node esbuild.mjs --e2e && node --test \"out/test/e2e/*.test.js\"",
    "dist:local": "node esbuild.mjs --production && electron-builder --mac dmg -c.mac.identity=- -c.mac.hardenedRuntime=false -c.mac.notarize=false -c.dmg.sign=false"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^44.4.5",
    "electron-builder": "^26.15.3",
    "esbuild": "^0.28.2",
    "monaco-editor": "^0.57.0",
    "pdfjs-dist": "^5.7.284",
    "typescript": "^7.0.2",
    "vscode-oniguruma": "^2.0.1",
    "vscode-textmate": "^9.3.2"
  },
  "author": {
    "name": "Oleg Arshinskii",
    "email": "velet592@gmail.com"
  }
}
